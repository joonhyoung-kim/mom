<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mom.XUMM1040">
	<select id="get_findBtn1" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">
		SELECT MPR.PR_NO
		     , MPR.PR_TYPE
		     , (SELECT MOM_COMMON_PKG.FN_GET_CODE_NAME(MPR.COMPANY_CD, MPR.DIVISION_CD, 'MM0001', MPR.PR_TYPE, 'KR') FROM DUAL)  AS PR_TYPE_NM
		     , MPR.PR_TITLE
		     , MPR.REFERENCE_NO
		      , MPR.REQUEST_DEPT_CD
		     , (SELECT MOM_COMMON_PKG.FN_GET_DEPARTMENT_NAME(MPR.COMPANY_CD, MPR.DIVISION_CD, MPR.REQUEST_DEPT_CD) FROM DUAL) AS REQUEST_DEPT_NM
		     , MPR.REQUEST_REASON
		     , (SELECT MOM_COMMON_PKG.FN_GET_CODE_NAME(MPR.COMPANY_CD, MPR.DIVISION_CD, 'MM0003', MPR.REQUEST_REASON, 'KR') FROM DUAL)  AS REQUEST_REASON_NM
		     , TO_CHAR(MPR.REQUEST_DATE ,'YYYY-MM-DD')     AS REQUEST_DATE
		     , MPR.REQUEST_USER_NO
		     , (SELECT MOM_COMMON_PKG.FN_GET_USER_NAME(MPR.COMPANY_CD, MPR.DIVISION_CD, MPR.REQUEST_USER_NO) FROM DUAL ) AS REQUEST_USER_NM
		     , MPR.EXPECTED_AMOUNT
		     , MPR.DESCRIPTION 
		     , MPR.COMPANY_CD
		     , MPR.DIVISION_CD
		FROM   MOM_PR_MST MPR
		WHERE  MPR.STATE = 'R'
		AND    MPR.COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
		AND    MPR.DIVISION_CD = #{divisionCd, jdbcType=VARCHAR}
	    AND    MPR.REQUEST_DATE BETWEEN TO_DATE(#{requestDateSD, jdbcType=VARCHAR}, 'YYYY-MM-DD')  AND TO_DATE(#{requestDateED, jdbcType=VARCHAR}, 'YYYY-MM-DD') + 0.99999  
	 
	    <if test="prNo != null and prNo != ''">
	    AND (UPPER(MPR.PR_NO) LIKE '%' || UPPER(#{prNo, jdbcType=VARCHAR}) || '%')	  
	    </if>	
	    <if test="referenceNo != null and referenceNo != ''">
		AND MPR.REFERENCE_NO = #{referenceNo, jdbcType=VARCHAR}
		</if>	
		<if test="requestDeptCd != null and requestDeptCd != ''">
		AND MPR.REQUEST_DEPT_CD IN (${requestDeptCd})
		</if>		
		 <if test="prTitle != null and prTitle != ''">
	    AND (UPPER(MPR.PR_TITLE) LIKE '%' || UPPER(#{prTitle, jdbcType=VARCHAR}) || '%')	  
	    </if>	
	
    ORDER BY MPR.PR_NO,MPR.PR_TYPE,MPR.REQUEST_DEPT_CD

	</select>
	<select id="get_findBtn2" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">
	SELECT MWR.WO_RESULT_ID
	 , MWR.WO_RESULT_ID AS KEY_ID
     , TO_CHAR(MWR.WORK_DAY ,'YYYY-MM-DD')   AS RESULT_CREATE_DATE
     , TO_CHAR(MWR.START_TIME ,'YYYY-MM-DD HH24:MI:SS') AS START_TIME
     , TO_CHAR(MWR.END_TIME ,'YYYY-MM-DD HH24:MI:SS')   AS END_TIME
     , MWR.WORK_CENTER_CD
     , (SELECT MOM_COMMON_PKG.FN_GET_WORK_CENTER_NAME(MWR.COMPANY_CD, MWR.DIVISION_CD, MWR.WORK_CENTER_CD) FROM DUAL) AS WORK_CENTER_NM
     , MWR.GOOD_QTY
     , MWR.DEFECT_QTY 
     , DECODE(MWR.STATE, 'C', (MWR.GOOD_QTY * -1), NULL) AS GOOD_CANCEL_QTY
     , DECODE(MWR.STATE, 'C', (MWR.DEFECT_QTY * -1), NULL) AS DEFECT_CANCEL_QTY
     , MWR.SHIFT_CD   AS SHIFT_DETAIL_CD
     , (SELECT MOM_COMMON_PKG.FN_GET_SHIFT_DETAIL_NAME(MWR.COMPANY_CD, MWR.DIVISION_CD, MWR.SHIFT_CD) FROM DUAL)  AS SHIFT_DETAIL_NM
     , MWR.STATE     AS RESULT_STATE
     , (SELECT MOM_COMMON_PKG.FN_GET_CODE_NAME(MWR.COMPANY_CD, MWR.DIVISION_CD, 'PP0009', MWR.STATE, 'KR') FROM DUAL)  AS RESULT_STATE_NM
     , MWR.CREATE_BY  AS RESULT_CREATE_BY
     ,(SELECT MOM_COMMON_PKG.FN_GET_USER_NAME(MWR.COMPANY_CD, MWR.DIVISION_CD, MWR.CREATE_BY) FROM DUAL ) AS RESULT_CREATE_BY_NM
     , MWR.UPDATE_BY  AS RESULT_CANCEL_BY
     ,(SELECT MOM_COMMON_PKG.FN_GET_USER_NAME(MWR.COMPANY_CD, MWR.DIVISION_CD, MWR.UPDATE_BY) FROM DUAL ) AS RESULT_CANCEL_BY_NM
     , TO_CHAR(MWR.CANCEL_DATE ,'YYYY-MM-DD')   AS RESULT_CANCEL_DATE 
  ,   MWR.DESCRIPTION
     , MWR.WORK_ORDER_ID
     , MWR.COMPANY_CD
     , MWR.DIVISION_CD
FROM   MOM_WO_RESULT MWR
WHERE  MWR.COMPANY_CD      = #{companyCd, jdbcType=VARCHAR}
AND    MWR.DIVISION_CD     = #{divisionCd, jdbcType=VARCHAR}
AND    MWR.WORK_ORDER_ID   = #{workOrderId, jdbcType=VARCHAR}
ORDER BY MWR.WO_RESULT_ID

</select>
	<insert id="create_customBtn1-1" parameterType="java.util.List">
        <foreach item="item" collection="list" index="i" separator=" " open="INSERT ALL" close="SELECT * FROM DUAL"> 
     INTO MOM_PROC_WO_RESULT_CANCEL (
                  COMPANY_CD
                , DIVISION_CD
                , WORK_ORDER_ID
                , WO_RESULT_ID
                , GOOD_QTY
                , DEFECT_QTY
                , DESCRIPTION
                , VAL_MSG
                , MULTI_MSG
                , CREATE_BY
                , CREATE_DATE )
      VALUES 
              (   #{item.companyCd, jdbcType=VARCHAR}
                , #{item.divisionCd, jdbcType=VARCHAR}
                , #{item.workOrderId, jdbcType=VARCHAR}
                , #{item.woResultId, jdbcType=VARCHAR}
                , #{item.resultGoodQty, jdbcType=NUMERIC}
                , #{item.resultDefectQty, jdbcType=NUMERIC}
                , #{item.description, jdbcType=VARCHAR}
                , #{item.valMsg, jdbcType=VARCHAR}
                , #{item.multiMsg, jdbcType=VARCHAR}
                , #{item.userId, jdbcType=VARCHAR}
                , MOM_COMMON_PKG.FN_GET_LOCAL_TIME(#{item.companyCd, jdbcType=VARCHAR},#{item.divisionCd, jdbcType=VARCHAR})
              )
   </foreach>
  </insert>
   <delete id="remove_customBtn1-1" parameterType="java.util.List">
       DELETE FROM MOM_PROC_WO_RESULT_CANCEL
      <where>
       <foreach collection="list" item="item" open="" close="" separator="OR">
        (       COMPANY_CD  = #{item.companyCd, jdbcType=VARCHAR}
           AND  DIVISION_CD = #{item.divisionCd, jdbcType=VARCHAR}
           AND  CREATE_BY   = #{item.userId, jdbcType=VARCHAR}
        )
       </foreach>
      </where>
   </delete>  
   <select id="proc_customBtn1-1" statementType="CALLABLE" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap">  
          { CALL MOM_WO_PKG.sp_cancel_wo_result(     		    #{p_err_code,  jdbcType=VARCHAR, mode=OUT}
					                                          , #{p_err_msg,   jdbcType=VARCHAR, mode=OUT}
					                                          , #{companyCd,   javaType=string,  jdbcType=VARCHAR , mode=IN}
					                                          , #{divisionCd,  jdbcType=VARCHAR, mode=IN}											  
					                                          , #{userId,      jdbcType=VARCHAR, mode=IN} )}    	              
 </select>  
</mapper>