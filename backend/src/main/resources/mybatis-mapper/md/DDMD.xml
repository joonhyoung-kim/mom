<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mom.DDMD">	
 <select id="get_DDMD0001" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT PLANT_CD  AS CODE 
        , PLANT_CD ||'('|| PLANT_NM  ||')' AS LABEL
   FROM   MOM_PLANT  
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY PLANT_NM 
</select>
<select id="get_DDMD0002" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT LOCATION_CD   AS CODE 
        , LOCATION_CD ||'('|| LOCATION_NM   ||')' AS LABEL
   FROM   MOM_LOCATION   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY LOCATION_NM 
</select>
   
<select id="get_DDMD0003" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT PARTNER_CD   AS CODE 
        , PARTNER_CD ||'('|| PARTNER_NM   ||')' AS LABEL
   FROM   MOM_PARTNER   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY PARTNER_NM  
</select>
<select id="get_DDMD0004" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT ITEM_ID    AS CODE 
        , ITEM_ID  ||'('|| ITEM_NM    ||')' AS LABEL
   FROM   MOM_ITEM   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY ITEM_NM    
</select>
<select id="get_DDMD0005" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT WORK_GROUP_CD    AS CODE 
        , WORK_GROUP_CD  ||'('|| WORK_GROUP_NM    ||')' AS LABEL
   FROM   MOM_WORK_GROUP   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY WORK_GROUP_NM        
</select>
<select id="get_DDMD0006" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT WORK_CENTER_CD     AS CODE 
        , WORK_CENTER_CD   ||'('|| WORK_CENTER_NM     ||')' AS LABEL
   FROM   MOM_WORK_CENTER   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY WORK_CENTER_NM    
</select>
<select id="get_DDMD0007" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
   SELECT WORK_STATION_CD     AS CODE 
        , WORK_STATION_CD   ||'('|| WORK_STATION_NM     ||')' AS LABEL
   FROM   MOM_WORK_STATION   
   WHERE  COMPANY_CD  = #{companyCd, jdbcType=VARCHAR}
   AND    DIVISION_CD = #{divisionCd, jdbcType=VARCHAR} 
   ORDER BY WORK_STATION_NM   
</select>      
<select id="get_DDMD0008" parameterType="java.util.HashMap" resultType="com.mom.dto.LowerHashMap" fetchSize="1000">   
    SELECT OPERATION_ID AS CODE 
         , OPERATION_ID   ||'('|| OPERATION_NM     ||')' AS LABEL
	FROM MOM_OPERATION
	ORDER BY OPERATION_NM
</select>        
</mapper>